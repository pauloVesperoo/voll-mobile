import { FormControl, IFormControlProps } from "native-base"
import { ReactNode } from "react"
interface EntradaTextoProps extends IFormControlProps{
    children: ReactNode
}

export function EntradaTexto({children, ...rest}: IFormControlProps){
    return(
        
    )
}